node_modules/
dist/
*.log
.DS_Store
.vscode/
.idea/

# Local environment (secrets, tokens). Copy .env.example to .env and edit locally.
.env
.env.local
.env.*.local

# Performance baseline: Lighthouse JSON report (regenerate with npx lighthouse ...)
**/lighthouse-report.json

## Local scratchpad (not project documentation)
docs/_NOTES.md

*storybook.log
storybook-static

# WebGPU golden harness failure diffs (opt-in script)
webgpu-golden-diff/
